# Deploying a Scalable ML Pipeline in Production.

Udacity Exercises and Demos from Course 4: Deploying a Scalable ML Pipeline in Production. 

Install requirements: `pip install -r requirements.txt` that include data version control package `dvc`.